/*
let examResults = [7,5,6,4,3,2,8]

function promedio(){
    let results = 0;
    for(i=0; i < examResults.length; i++){
        results = results + examResults[i];
    }
    return results / (examResults.length)
}

console.log(promedio(examResults))
*/

let resultado = 0
function sumar(primerNumero, segundoNumero){
    resultado = primerNumero + segundoNumero;
   

}
resultado = sumar(6,6);
console.log(resultado)